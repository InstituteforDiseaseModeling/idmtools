// .vscode/launch.json
{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Python: File (Run from file directory)",
        "type": "python",
        "request": "launch",
        "program": "${file}",
        "cwd": "${fileDirname}",
        "console": "integratedTerminal"
      }
    ]
  }
