"""Handles interaction with scancel command."""
from typing import Dict


def command_scancel(info: Dict):
    """
    Interacts with the scancel command.

    Args:
        info: Info on what to cancel

    Returns:
        Result from cancel
    """
    pass
